#Discord logger for some events, made for single server use as of now.

Create a config.json file

Token: bot token
PBToken: pastebin token

The rest is for the events respective channels, should be self explanatory.